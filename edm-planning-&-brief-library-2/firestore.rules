rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // No-auth mode: Public access with URL for EDM Planning tool
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
